extends layout

block script
  script(type="text/javascript").
    $(document).ready(function() {
      event = !{event};

      $("#eventDescription h2").text(event.title);
      $("#eventDescription h3").text(event.description);
    });

block content
  div(class="row")
    div(id="eventTitle", class="col-sm-10 col-sm-offset-1")
      h2
      h3